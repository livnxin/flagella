apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prediction.fullname" . }}-test-connection"
  labels:
    {{- include "prediction.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prediction.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
