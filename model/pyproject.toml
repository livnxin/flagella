[project]
name = "model"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "livnxin", email = "lilixinxin@protonmail.com" }
]
requires-python = ">=3.13.15"
dependencies = [
    "keras>=3.15.1",
    "kitops>=1.5.0",
    "mlflow>=3.16.0",
    "scikit-learn>=1.9.0",
    "tensorboard>=2.21.0",
    "tensorflow",
]

[project.scripts]
model = "model:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
